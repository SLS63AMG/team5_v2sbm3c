<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.pricehistory.PriceHistoryMapper">

    <!-- 모든 가격 기록 조회 -->
    <select id="getAllPriceHistories" resultType="map">
        SELECT historyno, historyprice, timestamp
        FROM pricehistory;
    </select>

    <!-- 특정 가격 기록 조회 -->
    <select id="getPriceHistoryByHistoryNo" parameterType="int" resultType="map">
        SELECT historyno, historyprice, timestamp
        FROM pricehistory
        WHERE historyno = #{historyno};
    </select>

    <!-- 가격 기록 삽입 -->
    <insert id="insertPriceHistory" parameterType="map">
        INSERT INTO pricehistory (historyno, historyprice, timestamp)
        VALUES (pricehistory_seq.NEXTVAL, #{historyprice}, #{timestamp});
    </insert>

    <!-- 가격 기록 수정 -->
    <update id="updatePriceHistory" parameterType="map">
        UPDATE pricehistory
        SET historyprice = #{historyprice},
            timestamp = #{timestamp}
        WHERE historyno = #{historyno};
    </update>

    <!-- 가격 기록 삭제 -->
    <delete id="deletePriceHistory" parameterType="int">
        DELETE FROM pricehistory
        WHERE historyno = #{historyno};
    </delete>

</mapper>
