<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- SB가 자동으로 구현하여 연동하는 interface -->
<mapper namespace="dev.mvc.answer.AnswerDAOInter"> <!-- SB가 자동으로 구현하여 연동하는 interface -->
  
  <insert id="answer" parameterType="dev.mvc.answer.AnswerVO">
    INSERT INTO answer(inquiryno, content, rdate, filename, inquiryno, memberno)
    VALUES(inquiry_seq.nextval, ${content}, SYSDATE, ${filename}, ${inquiryno}, ${memberno})
  </insert>

  
</mapper>