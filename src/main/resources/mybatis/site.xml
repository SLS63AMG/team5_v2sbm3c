<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.site.SiteMapper">

    <!-- 특정 사이트 정보 조회 -->
    <select id="getSiteBySiteNo" parameterType="int" resultType="map">
        SELECT siteno, sitename, siteurl
        FROM site
        WHERE siteno = #{siteno};
    </select>

    <!-- 모든 사이트 정보 조회 -->
    <select id="getAllSites" resultType="map">
        SELECT siteno, sitename, siteurl
        FROM site;
    </select>

    <!-- 새로운 사이트 삽입 -->
    <insert id="insertSite" parameterType="map">
        INSERT INTO site (siteno, sitename, siteurl)
        VALUES (site_seq.NEXTVAL, #{sitename}, #{siteurl});
    </insert>

    <!-- 사이트 정보 수정 -->
    <update id="updateSite" parameterType="map">
        UPDATE site
        SET sitename = #{sitename},
            siteurl = #{siteurl}
        WHERE siteno = #{siteno};
    </update>

    <!-- 사이트 삭제 -->
    <delete id="deleteSite" parameterType="int">
        DELETE FROM site
        WHERE siteno = #{siteno};
    </delete>

</mapper>
