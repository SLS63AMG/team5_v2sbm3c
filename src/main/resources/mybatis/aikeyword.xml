<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.aikeyword.AiKeywordMapper">

    <!-- 특정 검색 키워드와 연관된 AI 추천 키워드 조회 -->
    <select id="getAiKeywordsBySearchNo" parameterType="int" resultType="map">
        SELECT aino, searchno
        FROM aikeyword
        WHERE searchno = #{searchno};
    </select>

    <!-- AI 추천 키워드 삽입 -->
    <insert id="insertAiKeyword" parameterType="map">
        INSERT INTO aikeyword (aino, searchno)
        VALUES (aikeyword_seq.NEXTVAL, #{searchno});
    </insert>

    <!-- AI 추천 키워드 삭제 -->
    <delete id="deleteAiKeyword" parameterType="int">
        DELETE FROM aikeyword
        WHERE aino = #{aino};
    </delete>

</mapper>
